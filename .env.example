# Copiar este archivo a ".env" y completar con valores reales.
# ".env" está en .gitignore: nunca debe commitearse.

# Contraseña del usuario de PostgreSQL (docker-compose)
POSTGRES_PASSWORD=changeme
